export type Car = {
  id: number;
  model: string;        
  location: string;     
  listingDate: string;  
  imageUrl: string;
  price: number;
  currency: string;
  per: string;
  status: string;
  totalprice: number;
  days: number;
};
